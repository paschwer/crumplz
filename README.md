# crumplz

Site marketing — Astro — déployé sur Cloudflare Pages.

## Dev

```bash
npm install
npm run dev
```

## Deploy

Push sur `staging` → staging auto.
Merge `staging` → `main` → production auto.
